# RPC - TU135,137,138,139

### Null Sessions

* <pre class="language-shellscript" data-line-numbers><code class="lang-shellscript">rpcclient -U "" &#x3C;ip> -N
  >>> srvinfo, enumdomains, querydominfo, netshareenumall, netsharegetinfo &#x3C;share>, getdompwinfo, enumdomusers, queryuser &#x3C;RID>
  net use \\host\ipc$ "" /u:""
  ## Brute Forcing User RIDs / Impacket-Samrdump.py
  for i in $(seq 500 1100);do rpcclient -N -U "" 10.129.14.128 -c "queryuser 0x$(printf '%x\n' $i)" | grep "User Name\|user_rid\|group_rid" &#x26;&#x26; echo "";done
  enumdomusers ## get users
  getdompwinfo ## password policy
  </code></pre>

### Authenticated

* <pre class="language-shellscript" data-line-numbers><code class="lang-shellscript">rpcclient -U &#x3C;username> --password &#x3C;password> &#x3C;ip> -W &#x3C;domain>
  >>> srvinfo, enumdomains, querydominfo, netshareenumall, netsharegetinfo &#x3C;share>, getdompwinfo, enumdomusers, queryuser &#x3C;RID>
  ## Brute Forcing User RIDs / Impacket-Samrdump.py
  for i in $(seq 500 1100);do rpcclient -N -U "" 10.129.14.128 -c "queryuser 0x$(printf '%x\n' $i)" | grep "User Name\|user_rid\|group_rid" &#x26;&#x26; echo "";done
  enumdomusers ## get users
  getdompwinfo ## password policy
  </code></pre>

### RID Cycling

* <pre class="language-shellscript" data-line-numbers><code class="lang-shellscript">for i in $(seq 500 2000); do echo "queryuser $i" | rpcclient -U "" -N &#x3C;ip> 2>/dev/null | grep -i "User Name"; done
  for i in $(seq 500 2000); do echo "queryuser $i" | rpcclient -U &#x3C;username> --password &#x3C;password> &#x3C;ip> -W &#x3C;domain> 2>/dev/null | grep -i "User Name"; done
  </code></pre>

### RID Brute Force

* <pre class="language-bash" data-line-numbers><code class="lang-bash">nxc smb &#x3C;DC-IP> -u 'guest' -p '' --rid-brute
  rpcclient -U "guest" &#x3C;DC-IP> -N
  lookupname &#x3C;username>
  enumdomusers ## get users
  </code></pre>

### Tools

* [rpcinfo](https://learn.microsoft.com/en-us/windows-server/administration/windows-commands/rpcinfo)
* [rpcclient](https://www.samba.org/samba/docs/current/man-html/rpcclient.1.html)
* [rpcdump.py](https://github.com/SecureAuthCorp/impacket/blob/master/examples/rpcdump.py)
