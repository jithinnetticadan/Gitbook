# Server-Side Template Injection (SSTI)

Templating engines like Mako, Twig, Jade, Smarty, Jinja2, Freemaker, Velocity - Attacker is able to use native template syntax to inject a malicious payload into a template. - Template engines are designed to generate web pages by combining fixed templates with volatile data. - Attackers inject arbitrary template directives to manipulate the template engine, enabling them to take complete control of the server. - How do server-side template injection vulnerabilities arise? \* Attacks can occur when user input is concatenated directly into a template, rather than passed in as data. \* Static templates that simply provide placeholders into which dynamic content is rendered are generally not vulnerable. \* $output = $twig->render("Dear {first\_name},", array("first\_name" => $user.first\_name) ); (Not Vulnerable) \* $output = $twig->render("Dear " . $\_GET\['name']); (Vulnerable) - Constructing a server-side template injection attack $ Detect \* Try fuzzing the template by injecting - $\{{<%\[%'"\}}%\
\* Use in-built wordlist \* SSTI occurs in two distinct contexts % Plaintext context - templates allow to freely input content either by using HTML tags or native syntax. Try ${7\*7} instead of checking for XSS payloads. % Code context - user input being placed within a template expression $ Identify the template engine \* Error messages from fuzzing the input parameters will reveal the engine. \* else manually try engine specific available online $ Exploit - Exploiting SSTI vulnerabilities $ Read - reading its documentation after identifying the template engine \* Lab - Read the doc for ERB template, execute/frame the payloads obtained from PayloadofAllThings \* Lab - Read the doc for Tornado template, import the os library and execute commands. $ Read about the security implications - outline all the potentially dangerous things that people should avoid doing with the template. \* Lab - Fuzz the parameter without default payloads to obtain error message and identify the template engine. Craft a payload following the template syntax on view the security concern mentioned in the documentation. $ Look for known exploits \* Lab - Fuzz the parameter to identify the template engine. Look for any active exploits available. - Explore \* Intruder provides a built-in wordlist for brute-forcing variable names. \* Template engines expose a "self" or "environment" object of some kind, which acts like a namespace containing all objects, methods & attributes supported. \* Websites will contain both built-in objects provided by the template and custom, site-specific objects that have been supplied by the web developer. \* Lab - Fuzz the parameter to identify the template engine. Use 'debug' payload to know the objects and exploit further using the template documentation. - Create a custom attack \* Template engine executes templates inside a sandbox, which can make exploitation difficult, or even impossible. \* Proceed with traditional auditing techniques by reviewing each function for exploitable behavior. $ Constructing a custom exploit using an object chain \* First step is to identify objects and methods to which you have access. \* Refer the documentation to understand which objects to target. \* Lab - Fuzz the parameter. Refer documentation to bypass the sandboxed environment and construct a payload to read a file. $ Constructing a custom exploit using developer-supplied objects \* Template engines run in a secure, locked-down environment by default. \* Developer-created objects that are exposed to the template can offer a further, less battle-hardened attack surface. \* Lab - Fuzz -> Identify template engine -> Deduce info from error messages and craft a payload. Refer solution.
